import java.util.ArrayList;

public class CustomPlayer extends Player {
    public CustomPlayer(String playerClass, int money, ArrayList<item> a, double maxHealth, boolean m, String name, boolean ren, String race, String raceArchetype, String classArchetype) throws Exception {
        super(playerClass, money, a, maxHealth, m, name, ren, race, raceArchetype, classArchetype);
    }
    //TODO add custom Player which allows complete customization
}
